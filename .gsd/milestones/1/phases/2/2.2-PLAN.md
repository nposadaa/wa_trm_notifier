---
phase: 2
plan: 2
wave: 1
---

# Plan 2.2: Template Logic & Orchestration

## Objective
Update the WhatsApp client to handle dynamic parameters and create an orchestrator script (`main.py`) to bridge the scraper and the API.

## Context
- .gsd/SPEC.md
- scraper.py
- whatsapp_client.py

## Tasks

<task type="auto">
  <name>Update WhatsApp Client</name>
  <files>whatsapp_client.py</files>
  <action>
    Update `send_template_message` to accept a `params` list:
    - Construct the `components` array with `type: "body"` and `parameters: [{"type": "text", "text": str(p)} for p in params]`.
  </action>
  <verify>.\venv\Scripts\python.exe -c "from whatsapp_client import WhatsAppClient; print(WhatsAppClient())"</verify>
  <done>Client class supports parameter injection.</done>
</task>

<task type="auto">
  <name>Create main.py Orchestrator</name>
  <files>main.py</files>
  <action>
    Create `main.py`:
    - Import `scrape_trm` and `WhatsAppClient`.
    - Retrieve data from scraper.
    - Call `client.send_template_message` with `trm_daily_official` and the scraped values (`date`, `trm`).
  </action>
  <verify>.\venv\Scripts\python.exe main.py (after template is Active)</verify>
  <done>Integration test results in a 200/201 response from Meta.</done>
</task>

## Success Criteria
- [ ] `main.py` can successfully orchestrate a full scrape-and-send cycle.
